// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: workspaces.sql

package generated

import (
	"context"
	"database/sql"
)

const addWorkspaceMember = `-- name: AddWorkspaceMember :exec
INSERT INTO workspace_members (public_id, workspace_id, user_id, role, joined_at)
VALUES (?, ?, ?, ?, NOW(3))
ON DUPLICATE KEY UPDATE enabled = TRUE, role = VALUES(role)
`

type AddWorkspaceMemberParams struct {
	PublicID    []byte               `json:"publicId"`
	WorkspaceID uint32               `json:"workspaceId"`
	UserID      uint32               `json:"userId"`
	Role        WorkspaceMembersRole `json:"role"`
}

func (q *Queries) AddWorkspaceMember(ctx context.Context, arg AddWorkspaceMemberParams) error {
	_, err := q.db.ExecContext(ctx, addWorkspaceMember,
		arg.PublicID,
		arg.WorkspaceID,
		arg.UserID,
		arg.Role,
	)
	return err
}

const ensureWorkspace = `-- name: EnsureWorkspace :execresult
INSERT INTO workspaces (public_id, slug, name, timezone, country)
VALUES (?, ?, ?, ?, ?)
ON DUPLICATE KEY UPDATE id = id
`

type EnsureWorkspaceParams struct {
	PublicID []byte         `json:"publicId"`
	Slug     string         `json:"slug"`
	Name     string         `json:"name"`
	Timezone string         `json:"timezone"`
	Country  sql.NullString `json:"country"`
}

// EnsureWorkspace is the standalone bootstrap: the first request creates
// the single workspace, and every later one finds it.
func (q *Queries) EnsureWorkspace(ctx context.Context, arg EnsureWorkspaceParams) (sql.Result, error) {
	return q.db.ExecContext(ctx, ensureWorkspace,
		arg.PublicID,
		arg.Slug,
		arg.Name,
		arg.Timezone,
		arg.Country,
	)
}

const getWorkspaceByID = `-- name: GetWorkspaceByID :one

SELECT id, public_id, slug, name, description, icon_url, timezone, country, working_days, working_hours_start, working_hours_end, sort_weight, notes, enabled, updated_at, created_at FROM workspaces WHERE id = ? AND enabled = TRUE
`

// Workspaces.
//
// This application runs one implicit workspace: it has no tenant picker,
// and every calendar belongs to the same scope. The column and the table
// are still real, because the contract scopes every shared row by them and
// a second product on the same database will not be single-tenant.
func (q *Queries) GetWorkspaceByID(ctx context.Context, id uint32) (Workspace, error) {
	row := q.db.QueryRowContext(ctx, getWorkspaceByID, id)
	var i Workspace
	err := row.Scan(
		&i.ID,
		&i.PublicID,
		&i.Slug,
		&i.Name,
		&i.Description,
		&i.IconURL,
		&i.Timezone,
		&i.Country,
		&i.WorkingDays,
		&i.WorkingHoursStart,
		&i.WorkingHoursEnd,
		&i.SortWeight,
		&i.Notes,
		&i.Enabled,
		&i.UpdatedAt,
		&i.CreatedAt,
	)
	return i, err
}

const getWorkspaceBySlug = `-- name: GetWorkspaceBySlug :one
SELECT id, public_id, slug, name, description, icon_url, timezone, country, working_days, working_hours_start, working_hours_end, sort_weight, notes, enabled, updated_at, created_at FROM workspaces WHERE slug = ? AND enabled = TRUE
`

func (q *Queries) GetWorkspaceBySlug(ctx context.Context, slug string) (Workspace, error) {
	row := q.db.QueryRowContext(ctx, getWorkspaceBySlug, slug)
	var i Workspace
	err := row.Scan(
		&i.ID,
		&i.PublicID,
		&i.Slug,
		&i.Name,
		&i.Description,
		&i.IconURL,
		&i.Timezone,
		&i.Country,
		&i.WorkingDays,
		&i.WorkingHoursStart,
		&i.WorkingHoursEnd,
		&i.SortWeight,
		&i.Notes,
		&i.Enabled,
		&i.UpdatedAt,
		&i.CreatedAt,
	)
	return i, err
}

const getWorkspaceMember = `-- name: GetWorkspaceMember :one
SELECT id, public_id, workspace_id, user_id, role, invited_by_user_id, invited_at, joined_at, sort_weight, notes, enabled, updated_at, created_at FROM workspace_members
WHERE workspace_id = ? AND user_id = ? AND enabled = TRUE
`

type GetWorkspaceMemberParams struct {
	WorkspaceID uint32 `json:"workspaceId"`
	UserID      uint32 `json:"userId"`
}

func (q *Queries) GetWorkspaceMember(ctx context.Context, arg GetWorkspaceMemberParams) (WorkspaceMember, error) {
	row := q.db.QueryRowContext(ctx, getWorkspaceMember, arg.WorkspaceID, arg.UserID)
	var i WorkspaceMember
	err := row.Scan(
		&i.ID,
		&i.PublicID,
		&i.WorkspaceID,
		&i.UserID,
		&i.Role,
		&i.InvitedByUserID,
		&i.InvitedAt,
		&i.JoinedAt,
		&i.SortWeight,
		&i.Notes,
		&i.Enabled,
		&i.UpdatedAt,
		&i.CreatedAt,
	)
	return i, err
}
